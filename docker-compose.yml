services:
  ai_trader:
    build: .
    image: ai_daytrader:latest
    container_name: ai_daytrader
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env                 # Mount secrets
      - ./logs:/app/logs                 # Persist logs
    environment:
      - PYTHONUNBUFFERED=1
    # Interactive mode to allow stopping with Ctrl+C easily if running attached
    stdin_open: true 
    tty: true
